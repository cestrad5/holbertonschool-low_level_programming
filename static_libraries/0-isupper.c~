#include "main.h"
/**
 * _isupper - cecks for uppercase character
 * @c: type a character
 * Return: 0
 */

int _isupper(int c)
{
	if ('A' <= c && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
