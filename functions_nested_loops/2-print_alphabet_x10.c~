#include "main.h"

/**
* main - Write a function that prints 10 times the alphabet
* in lowercase, followed by a new line
* Return: Always 0 (Success)
* print_alphabet_x10 -  is so cool
*/
void print_alphabet_x10(void)
{
int n, t;
t = 0;
while (t < 10)
{
for (n = 'a'; n <= 'z'; n++)
{
_putchar(n);
}
t++;
_putchar('\n');
}
}
