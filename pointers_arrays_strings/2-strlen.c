#include "main.h"
/**
  * _strlen - returns the length of a string
  *
  * @t counter
  * Return: Srtring length
  */
void _strlen(char *t)
{
	int temp = 0;

for (; *t != '\0'; t++)
{
temp++
}
return (temp);
}
