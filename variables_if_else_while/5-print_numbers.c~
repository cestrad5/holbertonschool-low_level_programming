#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Prints the numbers 0 to 9.
*
* Return: Always 0.
*/
int main(void)
{
char n;
n = 0;
for (n = 0; n <= 9; n++)
putchar(n);
putchar('\n');
return (0);
}
