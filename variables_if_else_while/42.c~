#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Prints the alphabet in lowercase.
*
* Return: Always 0.
*/
int main(void)
{
char x;
x = 'a';
if ('a'<=x<='z' && x!='e' && x!='q')
{
putchar(x);
 x++;
 }
putchar('\n');
return (0);
}
