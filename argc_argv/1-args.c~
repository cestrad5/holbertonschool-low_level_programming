#include <stdio.h>

/**
 * main - a funtion to prints its name
 * @argc: argc value
 * @argv: the array
 * Return: 0 for success
 */

int main(int argc __attribute__((unused)), char *argv[])
{
printf("%d\n", argc);
return (0);
}
